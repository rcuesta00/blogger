<h2>All tags</h2>
<ul id="tags">
    <% @tags.each do |tag| %>
        <li><%= link_to tag.name, tag_path(tag) %><%= "   " %><%= link_to "<-delete", tag_path(tag), method: :delete, data: {confirm: "Really delete the tag?"} %></li>
    <% end %>
</ul>
<br>
<%= link_to "<< Back to Articles List", articles_path %>
